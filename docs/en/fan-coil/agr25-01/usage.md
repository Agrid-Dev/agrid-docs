# Usage

## Local Control via Touchscreen

From the touchscreen, the user can:

- Turn the thermostat **ON / OFF**
- Adjust the **temperature setpoint**
- Select mode: **Heating** (Heat) / **Cooling** (Cool) / **Fan only** / **Auto**
- Select fan speed: **Low** / **Medium** / **High** / **Auto**

## Remote Control via WiFi

The thermostat can also be controlled remotely via WiFi 2.4 GHz and the AGRID application.

!!! note "Autonomous Operation"
    In case of WiFi loss, the thermostat continues in local standalone mode.

## Security and Data

### WiFi Configuration

WiFi credentials are configured during commissioning via the AGRID Installer application. Each thermostat has unique credentials.

### Firmware Updates

Software updates are performed remotely (OTA) via the AGRID server in a secure manner.

### Factory Reset

A complete reset of the thermostat is possible from the settings screen or via the AGRID application. This operation erases all personal data (WiFi credentials, configuration) and restores factory settings.
